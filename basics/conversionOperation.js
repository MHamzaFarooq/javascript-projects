// what is type conversion?
// when we don't know the value's datatype we use type conversion method

let number = 3 + 3 // this will give us a NaN(Not a Numeber) value 
let numberType = Boolean(number)
console.log(numberType);
